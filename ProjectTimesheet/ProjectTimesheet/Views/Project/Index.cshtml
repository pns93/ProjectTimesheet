@model IEnumerable<ProjectTimesheet.ViewModel.ProjectViewModel>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Project</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
@*            <th>
                @Html.DisplayNameFor(model => model.IdProject)
            </th>
*@            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
@*            <th>
                @Html.DisplayNameFor(model => model.IdPm)
            </th>
*@            <th>
                @Html.DisplayNameFor(model => model.ProjectManagerName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StartDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EndDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
@*            <td>
                @Html.DisplayFor(modelItem => item.IdProject)
            </td>
*@            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
@*            <td>
                @Html.DisplayFor(modelItem => item.IdPm)
            </td>
*@            <td>
                @Html.DisplayFor(modelItem => item.ProjectManagerName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StartDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EndDate)
            </td>
            <td>
                    @if (User.IsInRole("Admin"))
                    {

                        @Html.ActionLink("Edit", "Edit", new { id=item.IdProject })
                    }
                    |

                @Html.ActionLink("Details", "Details", new {  id=item.IdProject  }) |
                    @if (User.IsInRole("Admin"))
                    {
                @Html.ActionLink("Delete", "Delete", new {  id=item.IdProject  })
                    }
            </td>
        </tr>
}
    </tbody>
</table>
